<h1>Hotels</h1>
<% @hotels.each do |hotel| %>
	<p><%= link_to hotel.name, hotel_path(hotel)%></p>
	<p><%=hotel.summary%></p>
	<p><%=hotel.city%></p>
	<% end %>
	 <h1>validates_presence_of :checkin_date<h1>
	 <h1>validate :future_checkin_date<h1>
